(function() {
    'use strict';
    
    if (window.lordfilm_bootstrap_loaded) return;
    
    function loadPlugin() {
        var version = Lampa.Manifest.app_digital >= 30000 ? 'v3' : 'v2';
        var url = 'https://' + window.location.hostname + '/lordfilm-plugin/' + version + '/plugin.js';
        
        Lampa.Utils.putScriptAsync(url, function() {
            console.log('[Lordfilm] Плагин версии', version, 'загружен');
        });
    }
    
    if (window.appready) {
        loadPlugin();
    } else {
        Lampa.Listener.follow('app', loadPlugin);
    }
    
    window.lordfilm_bootstrap_loaded = true;
})();
